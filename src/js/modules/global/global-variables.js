//* ===============================================================================================
//* VARIABLES GLOBALES
//* ===============================================================================================
const d = document;
const w = window;

const starIcon = '⭐';
const sunIcon = '☀️';
const moonIcon = '🌙';

const globalVariables = {
 arrayUsers: [],
 page: 1,
 moviesForPage: 10,
};

export { d, w, starIcon, sunIcon, moonIcon, globalVariables };
//* ===============================================================================================
